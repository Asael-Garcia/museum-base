import 'package:flutter/material.dart';

class MapMuseum extends StatelessWidget {
  const MapMuseum({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}