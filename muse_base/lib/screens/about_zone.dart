import 'package:flutter/material.dart';

class AboutZone extends StatelessWidget {
  const AboutZone({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}