import 'package:flutter/material.dart';

class AboutMuseum extends StatelessWidget {
  const AboutMuseum({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}